<network-security-config>
    <!-- MainActivity rejects HTTP unless the server host is private/local. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
